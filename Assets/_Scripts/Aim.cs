using UnityEngine;

public class Aim : MonoBehaviour {
    [SerializeField] private GameObject aimObject;

    void Update() {
        // var pos = Camera.main.ScreenPointToRay(new Vector3(Mouse.current.position.val, Mouse.current.position.y));
    }
}